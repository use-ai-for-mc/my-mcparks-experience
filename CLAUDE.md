# MCParks Experience Mod - Development Instructions

## Build and Deploy
Always use the `build-and-deploy.sh` script to build and deploy the mod:
```bash
./build-and-deploy.sh
```

This script handles both building and copying the jar to the correct mods folder.
